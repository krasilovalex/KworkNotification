````markdown
# 💼 KworkNoti — Telegram-бот для отслеживания новых заказов на Kwork

KworkNoti — это Telegram-бот, который мониторит новые заказы на [Kwork](https://kwork.ru/) в категории «Программирование» и автоматически рассылает их всем пользователям, нажавшим `/start` в боте.  

Дополнительно у каждого проекта есть кнопка **«Откликнуться»**, которая отправляет админу уведомление с информацией о пользователе и ссылкой на заказ.

> ⚠️ Проект сделан в учебных/портфолио-целях. Перед использованием в продакшене стоит внимательно ознакомиться с условиями использования Kwork и не нарушать их правила.

---

## ✨ Возможности

- 🔄 Периодический опрос Kwork через их внутренний API (POST `/projects`).
- 👥 Хранение подписчиков в SQLite (только chat_id и флаг активности).
- 📬 Рассылка **новых** проектов (по ID) всем подписчикам.
- 🧠 Защита от дублей — один и тот же заказ не шлётся повторно.
- 🔔 Кнопка «Откликнуться» под каждым проектом:
  - пользователь жмёт кнопку;
  - админ получает сообщение: кто откликнулся и на какой проект.
- 🧱 Структура, готовая к расширению:
  - фильтры по ключевым словам,
  - дневные дайджесты,
  - разные категории Kwork и режимы уведомлений.

---

## 🛠 Стек

- **Язык:** Python 3.12
- **Telegram:** [aiogram 3.x](https://docs.aiogram.dev/)
- **HTTP:** `aiohttp`
- **База данных:** `sqlite3` (файл `kwork_bot.db`)
- **Загрузка конфигурации:** `python-dotenv` (через `.env`)

---

## 📁 Структура проекта

Примерная структура репозитория:

```bash
KworkNoti/
│
├── app/
│   ├── main.py           # Точка входа, инициализация бота и фоновых задач
│   ├── config.py         # Чтение .env, константы (BOT_TOKEN, ADMIN_ID и т.д.)
│   ├── db.py             # Работа с SQLite (подписчики, проекты)
│   ├── models.py         # Dataclass Project
│   ├── handlers.py       # Хендлеры /start, /probe, callback "respond"
│   ├── kwork_parser.py   # Работа с Kwork API, получение списка проектов
│   ├── kwork_service.py  # Логика поиска новых проектов и рассылки
│   └── __init__.py
│
├── kwork_bot.db          # SQLite-база (создаётся автоматически)
├── .env                  # Конфиг с токеном бота и прочим (в .gitignore)
├── requirements.txt
└── README.md
````

---

## ⚙️ Настройка и запуск

### 1. Создать бота в BotFather

1. В Telegram: `@BotFather` → `/newbot`
2. Скопировать `BOT_TOKEN`.

---

### 2. Клонировать репозиторий

```bash
git clone https://github.com/<your-username>/KworkNoti.git
cd KworkNoti
```

---

### 3. Создать и активировать виртуальное окружение

```bash
python -m venv venv
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux / macOS
```

---

### 4. Установить зависимости

```bash
pip install -r requirements.txt
```

`requirements.txt` может выглядеть, например, так:

```text
aiogram>=3.7.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
```

(Если используешь loguru/orjson — добавь и их.)

---

### 5. Создать файл `.env`

В корне репозитория создай `.env`:

```env
BOT_TOKEN=твой_токен_бота_от_BotFather
ADMIN_ID=123456789        # твой Telegram ID (чтобы получать отклики)

# Необязательно. Если Kwork начнет требовать авторизацию для API:
# KWORK_COOKIE=сюда_значение_куки_из_DevTools
```

> ⚠️ `.env` должен быть в `.gitignore`, чтобы не залить токен и куки в публичный репо.

---

### 6. Запустить бота

```bash
python -m app.main
```

В логах увидишь что-то вроде:

```text
🚀 Бот запущен. Нажми Ctrl+C для остановки.
🔁 Фоновый мониторинг Kwork запущен
[kwork_parser] Найдено проектов: 12
📬 Найдено и разослано новых проектов: 12
```

---

## 🤖 Команды бота

* `/start`
  Подписывает пользователя на рассылку новых проектов.
  При первом запуске создаётся таблица `subscribers` в SQLite.

* `/probe`
  Ручная проверка: бот один раз сходить на Kwork, возьмёт минимум один проект и отправит его в чат для проверки работоспособности парсера/рассылки.

(Можно добавить и другие команды — `/help`, `/settings`, `/stop` и т.д.)

---

## 🔍 Как это работает внутри

### 1. Получение проектов с Kwork

Файл `app/kwork_parser.py`:

* Использует `aiohttp` и реальные эндпоинты Kwork.
* Делаем `POST` на `https://kwork.ru/projects` с полями:

  * `c` — ID категории (по умолчанию `"11"` — программирование),
  * `page` — номер страницы (`"1"`).
* Ответ — JSON с полем `data["wants"]`, где каждый `want` — это заказ.

Каждый заказ мапится в `Project`:

```python
@dataclass
class Project:
    project_id: str
    title: str
    price: str
    url: str
```

---

### 2. Хранение подписчиков и проектов

Файл `app/db.py`:

* Таблица `subscribers(chat_id PRIMARY KEY, is_active INTEGER)`.
* Таблица `projects(project_id PRIMARY KEY, title, price, url, created_at, notified)`.

Функции:

* `add_subscriber(chat_id)` — добавляет/активирует подписчика.
* `project_exists(project_id)` — проверка, был ли проект уже сохранён.
* `save_project(project)` — вставка нового проекта.
* `get_all_subscribers()` — все активные подписчики.

---

### 3. Поиск новых проектов и рассылка

`app/kwork_service.py`:

* `fetch_new_projects()`:

  * вызывает `get_projects()` из `kwork_parser`;
  * фильтрует по `project_exists`;
  * новые сохраняет в БД;
  * возвращает список новых проектов.

* `broadcast_new_projects(bot)`:

  * забирает всех подписчиков;
  * для каждого проекта формирует текст и Inline-клавиатуру:

    * кнопка **«🔔 Откликнуться»** (callback `respond:<project_id>`)
    * кнопка **«🌐 Открыть на Kwork»** (прямая ссылка)
  * отправляет проект всем подписчикам.

---

### 4. Отклик на проект

В `handlers.py`:

* Callback-хендлер на `respond:<project_id>`:

  * достаёт проект из БД по `project_id`;
  * формирует сообщение:

    * кто откликнулся (ссылка `tg://user?id=...`);
    * какой проект (название, цена, ссылка);
  * отправляет это сообщение админу (`ADMIN_ID`);
  * показывает пользователю alert:
    *«Я передал ваш отклик админу 👌»*

Таким образом кнопка **«Откликнуться»** — это сигнал системе/админу, что этот человек заинтересован в заказе.

---

## 💡 Идеи для доработки

* 📩 **Дневные дайджесты:** раз в день собирать все новые проекты и отправлять одним списком.
* 🧷 **Фильтр по ключевым словам:** хранить у пользователя список тегов (`python`, `telegram`, `ai`) и слать только релевантные заказы.
* ⚙️ **Меню /settings:**

  * режим уведомлений: «сразу» / «раз в день»;
  * ключевые слова;
  * отключение уведомлений.
* 📊 **Статистика:**

  * сколько раз пользователь откликался;
  * какие проекты были самыми «популярными».
* 🌐 **Web-панель для админа** (Flask/FastAPI) для просмотра проектов и откликов.

---

## 📝 Лицензия

Проект сделан для личного использования и портфолио.
Перед коммерческим использованием убедитесь, что соблюдаете правила и условия сервиса Kwork.
